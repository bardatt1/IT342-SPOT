// Basic test for utils.ts
describe('Basic Utils Test', () => {
  test('true is truthy', () => {
    expect(true).toBeTruthy();
  });
  
  test('1 + 1 equals 2', () => {
    expect(1 + 1).toBe(2);
  });
});
